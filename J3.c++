#include<bits/stdc++.h>

using namespace std; 
 
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    int n; 
    cin>>n; 

    for(int i = 0; i<0; i++){
        char curr[n][5];
        for(int j =0; j<5; j++){
            cin>>curr[i][j];
        }
    }


    for(int i = 0; i<n; i++){
        for(int j = 0; j<5; j++){
            
        }
    }

    return 0; 
    }
    
